const ParallelFetchingPage = () => {
    return (
        <div>
            <h1>Parallel Fetching Page</h1>
            <p>This is the base page for parallel data fetching.</p>
        </div>
    );
};

export default ParallelFetchingPage;